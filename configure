sudo CXX=g++          # The C++ compiler
sudo CXXFLAGS=-g       # C++ complilation flags
sudo NATIVE=on        # compiles code targeted to current hardware
sudo TUNE=generic     # performance-tuning switch
